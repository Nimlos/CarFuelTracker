package com.enr.carfueltracker.jpa.repository;

import com.enr.carfueltracker.jpa.entity.VolumeUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VolumeUnitRepository extends JpaRepository<VolumeUnit, Integer> {
}
